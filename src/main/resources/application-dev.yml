# 开发环境配置
spring:
  # H2 控制台 - 仅开发环境开启
  h2:
    console:
      enabled: true

  # 开发环境使用内存数据库
  datasource:
    url: jdbc:h2:mem:alerthub;DB_CLOSE_DELAY=-1
    username: sa
    password:

# 开发环境详细日志
logging:
  level:
    com.alerthub: DEBUG
    org.hibernate.SQL: DEBUG
